# Locked Python package set for the FFmpeg-only compression/decompression pipeline.
# torch and torchvision are installed separately from the PyTorch CUDA wheel index.
#
# Keep this file focused on direct runtime dependencies only. Their transitive
# dependencies are resolved by pip and do not need to be pinned here.

numpy==2.2.6
opencv-python==4.13.0.92
onnx==1.21.0
onnxruntime-gpu==1.23.2
pillow==12.1.1
pyyaml==6.0.3
requests==2.33.1
scipy==1.15.3
ultralytics==8.4.33
