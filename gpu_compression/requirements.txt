# Python dependencies for gpu_based_compression.
# NOTE: PyTorch is NOT listed here -- it comes pre-installed in the
# Jetson L4T base image and must not be overwritten by pip.

pyyaml>=6.0
opencv-python-headless>=4.8
ultralytics>=8.1
numpy>=1.24
pybind11>=2.11
